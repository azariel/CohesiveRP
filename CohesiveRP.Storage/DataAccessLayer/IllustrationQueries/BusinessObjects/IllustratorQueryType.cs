namespace CohesiveRP.Storage.DataAccessLayer.InteractiveUserInputQueries.BusinessObjects
{
    public enum IllustratorQueryType
    {
        CharacterMainAvatarGeneration = 0,// Generate a standard, generic Avatar for a character.
        CharacterExpressionAvatarFromSourceGeneration = 1,// Generate a specific expression using a sourceImage and a Seed.
        //SceneBackgroundGeneration = 2,// Generate a background for a scene.
        //SceneActionsGeneration = 3,// Generate an image representing the actions in a scene.
    }
}
