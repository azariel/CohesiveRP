namespace CohesiveRP.Common
{
    // Too many options here kills the options, especially with SDXL or other image generation. Keep it simpler
    public enum BreastsSize
    {
        Flat = 0,
        Small = 1,
        Average = 2,
        Large = 3,
        VeryLarge = 4,
        Enormous = 5,
    }
}
